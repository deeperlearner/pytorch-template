{
    "n_gpu": 1,
    "name": "Mnist_LeNet",
    "data_loaders": {
        "dataloader": {
            "module": "mnist_loader",
            "type": "MnistDataLoader",
            "args": {
                "train_dir": null,
                "valid_dir": null,
                "test_dir": null,
                "train_label": null,
                "valid_label": null,
                "test_label": null,
                "batch_size": 32,
                "shuffle": true,
                "num_workers": 2
            }
        }
    },
    "models": {
        "mnist": {
            "module": "MNIST",
            "type": "MnistModel",
            "args": {
            }
        }
    },
    "losses": {
        "loss": {
            "type": "nll_loss",
            "args": {
            }
        }
    },
    "metrics": [
        "accuracy", "top_k_acc"
    ],
    "optimizers": {
        "mnist": {
            "type": "Adam",
            "args": {
                "lr": 0.001
            }
        }
    },
    "lr_schedulers": {
        "mnist": {
            "type": "StepLR",
            "args": {
                "step_size": 50,
                "gamma": 0.1
            }
        }
    },
    "trainer": {
        "module": "mnist_trainer",
        "type": "Mnist_Trainer",

        "epochs": 2,
        "len_epoch": null,

        "save_period": 1,
        "verbosity": 2,

        "monitor": "min val_loss",
        "early_stop": 10,

        "tensorboard": true,

        "save_dir": "saved/",
        "log_config": "logger/logger_config.json"
    }
}
